# 复制成 .env 使用（.env 已在 .gitignore 里，不会被提交）。
# Mend 会读取仓库根目录的 .env，但只填「环境变量里还没有的键」，不会覆盖你已 export 的值。
# 也可以直接 export / set，不用这个文件。

MEND_API_KEY=sk-你的密钥

# 可选：默认走 DeepSeek，任何 OpenAI 兼容端点都能接（Qwen / GLM / Kimi / OpenAI / 本地 vLLM）
# MEND_MODEL=deepseek-chat
# MEND_BASE_URL=https://api.deepseek.com/v1
