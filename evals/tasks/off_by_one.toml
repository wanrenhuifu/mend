id = "off_by_one"
description = "calc.py 里的 average() 算错了：请修好它，不要修改测试。"
fixture = "evals/fixtures/off_by_one"
test_command = "python -m pytest -q"
expect = "pass"
fake_script = "evals/fixtures/off_by_one/fake_script.json"
